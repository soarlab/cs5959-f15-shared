The test file runs a fuzzer for base64.

Simply run:
python test.py 

in the same folder as the base64 binary.

This will start the fuzzer.  It will run, putting errors in the log.txt file, until interrupted with a ctrl-c.
